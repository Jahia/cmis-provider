describe('Mount tests', () => {
    after(() => {
        cy.logout();
    });

    beforeEach(() => {
        cy.loginEditor();
    });

    it('It creates mount point', () => {
        // TODO
    });
});
